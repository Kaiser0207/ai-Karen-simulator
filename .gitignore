# 環境變數 / 機密(絕對不要 commit)
.env

# Python
__pycache__/
*.py[cod]
.venv/

# 執行期產物
*.db
*.db-shm
*.db-wal
logs/
# logs 遷移備份,本機保留、不進 repo
logs_backup_*.tar.gz

# 背景音樂:只 commit 轉好的 mp3(~12MB);原始 wav(~125MB)留本機、不進 repo
music/*.wav
