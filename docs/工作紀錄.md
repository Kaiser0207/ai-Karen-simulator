# 工作紀錄(從 2026-06-10 起)

> 記錄每次做了什麼、動到哪些檔,方便日後回顧。最新的放最上面。

---

## 2026-06-11
- **修「背景音樂播放時麥克風開麥延遲」**:背景音樂在播時按麥克風要等一段才收音。根因:`getUserMedia({audio:true})` 預設**開回音消除**,音樂在播時瀏覽器得先建立/收斂「把喇叭音樂從麥克風扣掉」的管線 → 延遲。**查 git 確認非今天改壞**:麥克風那行自 06/09(`e9db187`)就沒改過、背景音樂 06/10(`3cc6bb1`)加入,是「音樂×麥克風」的交互;會不會感覺到延遲還取決於裝置(藍牙/喇叭最明顯)。修法:`getUserMedia` 關掉 `echoCancellation`(保留降噪/自動增益,STT 仍乾淨)→ 開麥不延遲;新增 `MUSIC.duckRec()`,錄音時把背景樂壓低(回音消除關了避免音樂漏進 STT)、錄完恢復。(`web/static/app.js`)
- **第二輪深度 bug 健檢 + 修我引入的三個回歸**:派兩個審查員深挖,抓到上一輪加 `/api/stall` 引入的三個回歸並修好:
  - **H-A(高)**:`applyStall` 全程沒設 `STATE.busy`、沒鎖輸入 → 超時 `await` 期間玩家又送出 `/api/say` 會同 thread 並發、`finishCustomer` 跑兩次(班次 results/計分重複)。修法:applyStall 比照 send 上鎖、`finishCustomer` 加冪等旗標。
  - **B2**:stall 砸店時 `emotion_history` 多補一筆 → storage 把回合數多算 1。修法:冷場不算有效回合,不補情緒(`anger_history` 仍補 100 給 final_anger)。
  - **M-A**:第一回合 stall 覆蓋了開局 `anger_history`。修法:只調怒氣、保留原始軌跡。
  - 健檢另發現尚未修:**B1**(mock 好客人永遠無法和解,mock-only)、**B3**(SER 標籤無正規化→拼字不一致時無聲失效)、**M-B**(打字+語音混合時語氣失真)、**Q3**(loader 不驗值範圍→壞關卡會中途炸 500)。
  - (`web/server.py` + `web/static/app.js`)

## 2026-06-10
- **修 #3:超時懲罰做成真的(後端真的加怒氣)**:新增 `/api/stall` 端點——超時冷場時顧客的**真實怒氣 +10**(寫進 graph state、不耗回合),累積到 100 **重用 graph 的 `set_ending`/`judge` 真的砸店**(報告含成本控制分,與正常砸店一致)。並修掉「**第一回合還沒打字就超時被誤判『本場已結束』**」(graph 尚無狀態時改用 session 開局怒氣)。前端 `onTimerExpire` 改呼叫它、血條改以伺服器真實怒氣為準(不再畫面假裝)。三情況測試通過。(`web/server.py` + `web/static/app.js`)
- **修 #1:429 重試風暴(健檢新發現)**:`_invoke_structured` 原本連 429/限流也重抽 3 次(**一次失敗空燒 ~6 次額度 + 卡 ~15 秒**,違反自己註解的分工)。抽出 `_is_rate_limited`,限流**立刻往外拋**、只重抽真正的格式錯誤。實測:格式錯誤→3 次、429→1 次。(`services/llm.py`)
- **學習教材 1~8 全面更新到現行版本**:6課→8課、Gemini→預設 Groq、主前端改 web/(Gradio 降備援)、補 `concession_cost`/`cost_spent`/`voice_emotion`、讓步成本/成本控制分、語氣影響奧客(SER)、評審標籤對話、SER 微服務、班次模式、存檔改一班次一檔。5 個並行更新員實際對照程式碼修改,改完驗證無殘留舊事實。(`docs/學習_1~8.md`)
- **清死欄位 C2**:8 個關卡 JSON 移除從沒被讀的 `avatar`(指向不存在的 `assets/`,立繪實際走 `char`)和 `tts_voice`(放棄的 TTS 預埋);順手修 `graph/state.py` 提到 avatar 的過時註解。逐行移除+寫入前驗證 JSON,保留手排格式;35 測試通過、8 關照常載入、立繪 20 張齊全不受影響。(`scenarios/*.json` + `graph/state.py`)
- **修結局兩句氣泡(L8)**:結局回合本來會同時畫「奧客即時回話」+「預設收尾台詞」兩個氣泡。改成**結局只顯示手寫的預設收尾**(一般回合仍顯示即時回話),保留各關 `ending_lines`、一個氣泡。(`web/static/app.js`)
- **修 M5:語音辨識逾時防卡死**:`/api/stt` 上傳從裸 `fetch` 改走 `api()`(內建 60 秒 AbortController 逾時),STT 後端卡住/網路停住時不再永遠卡在「辨識中…」,逾時會還原麥克風並提示。(`web/static/app.js`)
- **修 M2:結束分支防呆**:`web/server.py` 結束那段把直接索引(`result["report"]`、`result["messages"][-1]`)改成 `.get` + 後備值(缺 report→空 dict、messages 空→通用收尾句),避免 graph 回傳不完整時崩成「裸 500」。正常流程行為不變,全套 35 測試通過。(`web/server.py`)
- **專案健檢**:三方向(後端 bug / 前端與 API / 死碼與功能)全面審查,產出 `docs/專案健檢報告.md`(列出 H1~H3 高優先、M/L 中低、可清死碼、可加功能,含建議處理順序)。
- **修 H1:結束後重送會污染歷史 + 重複花 LLM**:`web/server.py` 的 `_get_session` 從 checkpointer 還原時,若該場 `ended` 已是 True 就回 None(擋掉),不再讓已結束的對局被同一 thread_id 的 `/api/say` 重跑。新增回歸測試 `tests/test_session_recovery.py`(3 例)、全套 35 測試通過。(`web/server.py` + `tests/test_session_recovery.py`)
- **開 demo 對外通道(裝了 ngrok)**:校內 server 對外連不到、且麥克風要 HTTPS 才能用,所以做外網 HTTPS 通道。cloudflared 的 `trycloudflare.com` 被校網擋掉(桌機 404/手機空白),**改用 ngrok 成功**(`https://xxxx.ngrok-free.app`,HTTPS、麥克風 OK)。**遊戲程式碼完全沒動,純外部工具。** demo 開法:`~/bin/ngrok http 8000` → 貼出網址(每次會變,首頁按 Visit Site)→ 用完 **Ctrl+C** 關。
  - 🔐 **之後要「安全還原」需刪除的檔(全都不在專案內)**:
    - `~/.config/ngrok/ngrok.yml` ← **裡面是 ngrok authtoken(等於密碼),最該刪**
    - `~/bin/ngrok` ← ngrok 程式本體(31M),要徹底移除就刪
    - `/tmp/ngrok.tgz`(12M 安裝檔)、`/tmp/ngrok.log`、`/tmp/cloudflared.log`、`/tmp/cloudflared2.log` ← 暫存檔,可刪
  - 一鍵清除:`rm -f ~/.config/ngrok/ngrok.yml ~/bin/ngrok /tmp/ngrok.tgz /tmp/ngrok.log /tmp/cloudflared*.log`
  - ⚠️ 系統的 `/bin/cloudflared` 是 root/poyu39 的服務,**不要刪**;`~/bin/cloudflared`、`ffmpeg` 等是本來就有的,也不是這次裝的。
- **語氣 → 奧客反應多元化**:把 `services/llm.py` 的 `_VOICE_TONE_RULE` 從「只調憤怒值」升級成「**同時決定奧客怎麼回話**」——店員語氣兇 / 心虛 / 溫和,奧客分別走「回嗆 / 得寸進尺 / 軟化試探」,不再每次反應都一樣。(`services/llm.py`)
- **LLM 供應商切換**:`.env` 改用 **Gemini 2.5-flash**(`thinking_budget=0` 關推理加速)。備註:Gemini 免費層**一天 20 次**,要實測可一行切回 Groq。
- **修評審「歸錯人」bug**:評審改用「**標籤對話**(`店員:` / `奧客:`)」整包送進 LLM,修掉「把奧客的話當成店員的話來扣分」(劉董那場 `compliance` 由錯誤的 10 → 正確的 80)。(`services/llm.py` + `prompts/judge_system.txt`)
- **logs 整理成「一班次一檔」**:`storage.py` 改寫——同班次合成 `logs/shift_<id>.json`(內含 `rounds[]`),單場進 `logs/solo/`;前端歷史頁不變。舊的 **32 個散檔**已搬進新結構並備份成 `logs_backup_20260610_*.tar.gz`。

---

## (下次照這個範本填)

```
## YYYY-MM-DD
- 做了什麼(動到哪些檔)
- 做了什麼(動到哪些檔)
```
